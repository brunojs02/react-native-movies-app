import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginVertical: 20,
  },
  textContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  listContainer: {
    marginVertical: 20,
  },
});

export { styles };
