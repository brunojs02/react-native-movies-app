import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginVertical: 0,
  },
  textContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  listContainer: {
    marginTop: 10,
  },
});

export { styles };
